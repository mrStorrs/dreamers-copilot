# Test benchmarks

Record measured successful runs at the project root in test-benchmarks.md; create after the first successful test run and commit with the change. Update each command's duration/date after every successful run, including fixes. Preserve human Notes.

Next timeout: max(last duration × 2, 30 seconds).

| Command | Last run | Updated | Recommended timeout | Notes |
| --- | --- | --- | --- | --- |
