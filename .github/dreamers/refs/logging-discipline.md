# Logging

Use project logging.instructions.md when present; otherwise follow the surrounding logger, levels, and format. Ask if neither gives a usable convention.

Never log secrets, tokens, PII, or complete request/response bodies. Keep logging changes within the approved scope. For a logging audit or new project standard, read [logging standards](../templates/logging-standards.md).
