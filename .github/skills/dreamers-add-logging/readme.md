# /dreamers-add-logging

Audit the requested scope against logging standards and project conventions. Approve the proposed changes, apply them, verify, optionally run Vigil, and commit locally.

Sensitive data must not appear in logs. Use --scope to limit the audit.

See [the workflow](SKILL.md).
