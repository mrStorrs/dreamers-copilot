# /dreamers-plan

Explicit detailed planning: clarify requirements, preserve the Grill transcript, write the smallest useful plan(s), then present the proposal and plans for one approval.

Standalone invocation stops after approval. When invoked by /dreamers, approval returns directly to implementation. The default /dreamers path instead saves the approved proposal without detailed planning.

See [the workflow](SKILL.md).
