# /dreamers-cleanup-comments-branch

Run the comment-cleanup workflow on the current feature branch's changed files, including pending edits. Requires a usable remote default-branch base.

The proposal and optional Vigil review gates remain; the final commit stays local.

See [the workflow](SKILL.md).
